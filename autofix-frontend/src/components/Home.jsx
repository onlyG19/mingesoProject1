import { Typography } from '@mui/material';

const Home = () => {
    return (
        <div>

        
        <Typography variant="h2" component="h2" sx={{ marginTop: '16px' }}>
            Autofix: Sistema de Reparacion de Vehículos
        </Typography>

        <Typography variant="h4" component="p" sx={{ marginBottom: '76px',marginTop: '16px' }}>
            Tu auto en las mejores manos
        </Typography>
        </div>
    );
};

export default Home;